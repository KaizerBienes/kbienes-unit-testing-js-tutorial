# kbienes-unit-testing-js-tutorial

## Getting started
- clone the repository
- run `npm install` to install all dependencies
- run tests with `npm test`
- generate coverage with `npm run coverage`

__WIP__
